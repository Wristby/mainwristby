## Packages
recharts | For financial charts and dashboards
framer-motion | For smooth animations and transitions
date-fns | For date formatting
clsx | For conditional class names (already in base but explicit is good)
tailwind-merge | For merging tailwind classes (already in base)

## Notes
Tailwind Config: extend colors for 'slate', 'emerald', 'amber' if not present by default (usually are).
Font: Inter (Google Fonts)
Authentication: Uses Replit Auth (useAuth hook already provided)
